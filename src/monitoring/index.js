import { getConfig, setConfig } from './config';
import { saveBehaviorLog, saveErrorLog, savePerformanceLog } from './logger';
import report from './report';

export {
  getConfig,
  setConfig,
  saveBehaviorLog,
  saveErrorLog,
  savePerformanceLog,
  report
};
